# Release Notes

> 按周记录重大变更。小修复/文档更新不列入。

---

## 2026-05-26 ~ 2026-06-01

- **初始版本**：A股量化模拟交易系统初始提交，31因子+8策略profile
- **核心引擎统一**：所有因子/评分/交易逻辑整合到 `core/`，回测和模拟盘共享
- **Position 领域模型**：新增 `to_dict`/`from_dict`/`holdings` 辅助方法
- **StrategyEngine 统一入口**：支持 factor/ensemble/ml/hybrid/multi 五种模式
- **多策略并行框架 (v12_multi)**：v11b(0.5) + v10c(0.3) + v6b(0.2)，z-score 标准化加权混合
- **v13 小资金中短线策略**：中证800+流动性过滤+5日反转+量价异动，WF 94%
- **v5 分级止盈 + 持仓衰减**：新增 `partial_sell`/`check_take_profit`/`apply_holding_decay`
- **ATR 自适应止损**：新增 `allocate_weights`
- **Barra 风格归因分析**：新增归因工具
- **风险平价权重分配**：新增权重分配模块
- **回测自检警告**：结果异常时自动告警
- **防静默误配置**：新增防御性检查
- **盘中双阶段模式**：上午信号+下午执行+日终报告
- **v6b_hlr 成为默认**：sim_daily 切换 v6b_8f → v6b_hlr
- **3 个短周期因子**：gap_ratio / high_low_range / intraday_drift
- **v9 短线策略退役**：freq=5 短线策略证伪
- **路径统一**：数据路径改为项目相对路径
- **买入取整**：所有买入操作统一 100 股整数倍
- **look-ahead bias 修复**：open 执行信号滞后 1 天
- **行业轮动调研**：申万行业分类+回测（接口不可用，暂缓）
- **ML 因子调研**：XGBoost 严重过拟合，样本外无效
- **趋势择时调研**：简单均线择时在 A 股无效
- **基本面因子调研**：v8/v9 回测框架
- **v8 WF 验证**：18 因子 vs 8 因子对比

---

## 2026-06-02 ~ 2026-06-08

- **v13 SL/TP 参数扫描**：90 组参数，最优 SL=1.5%/TP=3%/hold=5，全量 68.25%/3.206/-12.97%，WF 100%
- **v13 评分排序制**：WF 从 69%→94%（15/16），全量 49.87%/2.484
- **v20c 软约束评分排序**：硬过滤→加权评分，选股率 24%→80%+，WF 15/16(94%)
- **v11b 优化探索**：
  - 分级止盈参数优化（采纳）
  - intersection/lowvol/novol/bear 变体 WF 均不通过
  - 结论：v11b WF 不通过是结构性问题，截面因子在熊市失效
- **v14_resid 残差动量**：WF 8/16，熊市 fold -69%，证伪
- **v15_quality 基本面质量因子**：全量 13.20%/0.64/-24.87%，证伪
- **v16 动量+反转混合**：全量 12.98%/0.56/-30.93%，两信号相互抵消，证伪
- **v17/v18/v19 三个新策略**：测试后归档
- **国泰君安 Alpha 191/144 因子调研**：文档记录
- **v20 尾盘缩量企稳策略**：WF 验证通过，模拟盘脚本+cron 调度
- **v20 改为尾盘选股+尾盘买入**
- **数据更新独立为 cron**：信号/执行脚本不再更新数据
- **科创板排除规则补充**：689xxx 前缀（九号公司）
- **中证800成分股更新**：含 69 只科创板，股票池对齐官方指数

---

## 2026-06-09 ~ 2026-06-15

- **v27 WF 通过**：mom_5>2% + pv_corr_20 价量共振 + gap/illiq/boll，WF 15/100% 正收益，夏普 8.66，已上线账户2
- **v27 向量化加速**：WF 选股 + NAV 计算 30x 加速（20s vs 超时）
- **v11b 向量化重构**：信号生成从分钟级降到 6 秒
- **v11b WF 验证脚本**：完整引擎版
- **v28/v29 新因子测试**：v28 滴水穿石（证伪）、v29 球队硬币（证伪）
- **v20c 参数优化**：WF 扫描最优 SL=-2%/TP=5%/hold=5 天
- **v27 参数更新**：TP=10%→5%，hold=8→5，max_buy=6→4
- **统一配置**：三个账户 TP/SL/hold_days 全部从 strategy_map 读取
- **封板判断优化**：卖一量+涨幅阈值双重判断
- **cron 监控脚本**：检测漏执行和无标记失败
- **市场状态识别**：新增动态仓位乘数，市场状态判断改用上证指数
- **v27 模拟盘脚本替代 v13**
- **账户管理优化**：自动建表+force 重建+文档补全
- **v20c 持仓周期 5天→2天，止盈 25%→15%**
- **v20c 流动性范围调整**：100万~2亿
- **选股时限制新股数量**：确保持仓不超 MAX_HOLDINGS
- **v27 选股时释放当天卖出仓位**：避免持仓超 MAX_HOLDINGS
- **信号输出补充股票名称**：buy_plan/sell_plan 加 name 字段
- **持仓文件加锁防并发冲突**：flock 保护
- **v28 Kronos AI 增强选股**：代码就绪，零样本对 A 股无效，待微调
- **v28 合并到 main 分支**

---

## 2026-06-16 ~ 2026-06-20

- **数据库拆分**：单一 `quant.db` → `quant_stocks.db`（股票池+K线+指标）+ `quant_accounts.db`（账户+持仓+交易日志），彻底隔离
- **回测框架重构**：新增 `strategy_adapter.py` + `wf_runner.py`，统一回测和模拟盘的选股/评分/交易逻辑
  - v27 WF 4/4 正收益，夏普 4.16
  - 面板数据顺序明确定义：`[close, volume, amount, open, high, low]`
- **账户-策略分离**：`account_runner.py` 重写为子命令模式（`list`/`create`/`switch`/`run --account-id N`），策略不再硬编码绑定账户
- **v20c 退役**：面板顺序 bug 修复后策略彻底失效（WF 5/16，全量 -67%），旧版好结果完全源于面板错位假阳性。已清理所有引用
- **v11b 暂停**：cron 禁用，代码保留，随时可恢复
- **LightGBM 因子调研完成**：5 个因子（REV_1D/STD60/ROC60/AMP_TREND/MA60DEV）实现并验证。单因子 IC 微弱，LightGBM 组合 Test IC=0.03（过拟合严重），不融合 v27
- **路径兼容**：所有活跃脚本改为项目相对路径（`__file__` 推断），不再需要 `PYTHONPATH` / `BACKTEST_DATA_DIR`
- **Editable Install**：新增 `pyproject.toml`，`pip install -e .` 一键安装
- **init_project.py**：一键初始化脚本，告别 CSV 依赖
- **96 个过时代码归档**到 `scripts/archive/`
- **数据校验**：`update_daily_data_async.py` 下载后校验价格>0、成交量>0、成交额≈收盘价×成交量，异常数据跳过并告警
- **Error Handling**：`account_runner.py` 的 `run_signal` / `run_execute` 加 try-except，失败时写 CRON_STATUS error，不再静默失败
- **持仓清理**：`load_account` 时自动检查退市/停牌持仓，超过 N 天无交易记录自动清除
- **交易日判断**：信号/执行前增加 `is_trade_day()` 检查，非交易日不运行；严格检查当天数据，不用前一天兜底
- **选股池统一**：新增 `get_tradeable_codes()`，统一回测和模拟盘的板块过滤逻辑（排除 688/689/8/4/2 开头），消除两处不一致
- **表现监控框架**：新增 `nav_compare.py`，模拟盘 vs 回测 NAV 对比（框架就绪，后续完善）
- **v31 归档**：v29→v31 重编号，与 v27 高度重叠，不独立回测
- **指数K线数据独立存储**：init_project 集成下载
- **CRON_SETUP.md 更新**：新版 run --account-id 语法
- **trade_log 表路由错误修复**：save_account 写入正确的库
- **argparse subcommand 修复**：account_runner.py 子命令与 positional 冲突解决
- **v20c WF 修复**：面板传参/仓位参数/初始资金/流动性阈值/amount 单位检查
- **v20c select score 0→1.0**：wf_runner 增加 BUY/SELL/SKIP print 输出
- **文档全面更新**：DEPLOY.md / USER_MANUAL.md / CRON_SETUP.md 同步架构变更

---

## 策略生命周期总览

| 策略 | 状态 | 说明 |
|------|------|------|
| v27 价量共振 | ✅ 运行中（账户2） | WF 100%，夏普 8.66，当前主力 |
| v11b 多组 Ensemble | ⏸️ 暂停 | WF 69%，结构性问题，代码保留 |
| v20c 尾盘缩量企稳 | ❌ 退役 | 面板顺序 bug 修复后失效 |
| v31 动量增强 | ❌ 归档 | 与 v27 高度重叠 |
| v28 Kronos AI | 🔬 调研完成 | 零样本无效，代码保留待微调 |
| v13 中短线反转 | ✅ WF 通过 | WF 94%，备用策略 |
| v10c/v6b/v12_multi | ❌ 证伪/退役 | 见 RESULTS_LOG |
